import React from 'react';
import { Outlet } from 'react-router-dom';


const P2pLayout = () => {
    return (
        <div>
            <Outlet></Outlet>
        </div>
    );
};

export default P2pLayout;