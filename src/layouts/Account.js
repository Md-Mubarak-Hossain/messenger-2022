import React from 'react';
import Login from '../account/Login/Login';

const Account = () => {
    return (
        <div>
            <Login></Login>
        </div>
    );
};

export default Account;